# Lambda ランタイム依存パッケージ（SAM ビルド用 / asken-garmin-sync 専用）
# - boto3 は Lambda 実行環境に組み込み済みのため除外（デプロイパッケージ肥大化防止）
# - ローカル開発時は pyproject.toml の [project.dependencies] を使用すること
# - garminconnect は Garmin 専用。MFP Lambda には不要のため requirements.txt を分離している。
garminconnect==0.3.2
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2
