# Lambda ランタイム依存パッケージ（SAM ビルド用 / asken-myfitnesspal-sync 専用）
# - boto3 は Lambda 実行環境に組み込み済みのため除外（デプロイパッケージ肥大化防止）
# - ローカル開発時は pyproject.toml の [project.dependencies] を使用すること
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2
